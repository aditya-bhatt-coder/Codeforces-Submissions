#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    int t;
    cin>>t;
    while(t--){

        int n, isadded=false;
        cin>>n;

        for(int i=n-1 ; i>=1 ; i++){
            cout<<i;
            if(isadded==false and i)
        }

    }

    return 0;
}